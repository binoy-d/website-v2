# Copy to `.env.deploy` (git-ignored). Used by `make deploy` / scripts/deploy.sh.
# SSH host (an alias from ~/.ssh/config works) and the directory on that host to deploy into.
# DEPLOY_DIR may be absolute, or relative to the remote user's home.
DEPLOY_HOST=your-ssh-host
DEPLOY_DIR=dev/website-v2
